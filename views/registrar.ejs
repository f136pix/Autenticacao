<%- include ('partials/header') %>

<!-- Trago o body para dentro do EJS especifico, para ser mais facil trocar o background -->
<body class="bg-warning">

    <div class="row w-50 text-center">
          <div class="card bg-dark text-white form ">
            <div class="card-body">
    
              <!-- Faz o HTTPS POST -->
              <form action="/registrar" method="POST">
                <div class="form-group mt-3">
                  <label for="email">Email</label>
                  <input type="email" class="form-control" name="email">
                </div>
                <div class="form-group mt-5">
                  <label for="senha">Senha</label>
                  <input type="senha" class="form-control" name="senha">
                </div>
                <button type="submit" class="btn btn-info mt-5 mb-3">Registrar</button>
              </form>
            </div>
        </div>
    </div>

<%- include ('partials/footer') %>